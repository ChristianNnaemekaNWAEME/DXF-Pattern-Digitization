# dxf-pattern-digitization

Standalone demonstration of converting legacy garment pattern
measurements into valid DXF (Drawing Exchange Format) CAD files,
and verifying the conversion is lossless by reading the file back.

See DXF_DIGITIZATION.md for full documentation and results.

Run with: `python dxf_digitization_demo.py`
